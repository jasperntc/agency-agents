{"agent": "engineering-payments-billing-engineer", "brief": "We're adding mid-cycle plan changes to our subscription billing. Write a module exposing:\n\n    proration(old_cents: int, new_cents: int, period_start: date, period_end: date, change_at: date) -> int\n\n`old_cents` and `new_cents` are the two plan prices for the whole billing period. The three dates are `datetime.date`. Return a single integer number of cents: positive means we charge the customer now, negative means we credit them. A customer who switches at the very start of the period should see the full difference between the two plan prices.", "domain": "billing", "module": "proration.py", "suite_sha256_at_registration": "8f478a8fbcedf6d31e8bd0f6372b82b16f37b0441735017dbed943a7da073e8d", "task": "c001", "why": "Money arithmetic has strong unwritten conventions -- symmetry of rounding, boundary handling, what to do with a date outside the period. None of it is in the brief."}
{"agent": "engineering-api-platform-engineer", "brief": "Write the pagination helper for our public API. Rows are dicts with `id` (a unique string) and `created_at` (an int epoch second), and the API returns them newest first. Expose:\n\n    page(rows: list[dict], cursor: str | None, limit: int) -> dict\n\n`rows` is the full result set in arbitrary order. `cursor` is None for the first page, otherwise a value this function previously returned. Return {\"items\": [...], \"next_cursor\": <str or None>} with at most `limit` items, newest first, and `next_cursor` set to None on the last page.", "domain": "api-platform", "module": "pagination.py", "suite_sha256_at_registration": "0667ffec50fabeb3a666b42dc4ec66c1e820a62a3b18b1c1806fbc63fa8e74ea", "task": "c002", "why": "Offset paging satisfies every stated requirement and breaks the moment a row is inserted. That distinction is the entire specialism and the brief cannot hint at it without giving the answer."}
{"agent": "engineering-data-engineer", "brief": "Write the merge step of our nightly warehouse load. Expose:\n\n    merge(existing: list[dict], batch: list[dict]) -> list[dict]\n\nEvery record is a dict with `key` (a unique string), `updated_at` (an int epoch second) and `value`. `existing` is the current table contents; `batch` is the file that just arrived. Return the new table contents.", "domain": "data-warehouse", "module": "loader.py", "suite_sha256_at_registration": "9fd86fab7be3ac8c5403492487e5ca2f1bccea2cfc91752b5e054daa10107ad3", "task": "c003", "why": "Replay safety, in-batch duplicates and out-of-order delivery are the daily reality of a loader and none of the three is stated. The record carries updated_at, which is the only signal present."}
{"agent": "engineering-privacy-engineer", "brief": "We need to hand a copy of our user table to an analytics vendor. Write a module exposing:\n\n    pseudonymise(record: dict, salt: str) -> dict\n\n`record` has `email` and `phone` fields plus arbitrary other fields. Return a dict with `email` and `phone` replaced by pseudonyms and every other field left alone. The vendor needs to count distinct users, so the same person must get the same pseudonym every time.", "domain": "privacy", "module": "pseudonymise.py", "suite_sha256_at_registration": "b03b8540852bc4516bc11085dcc3735a6e7a72d2420ccf658eaa7387cda8e6a8", "task": "c004", "why": "The brief says what a pseudonym is for and never says what it must resist. Reversible encodings and partial masks pass every stated requirement."}
{"agent": "engineering-i18n-engineer", "brief": "Our UI strings are going multilingual. Write a module exposing:\n\n    plural_category(n: int, locale: str) -> str\n\nReturn the CLDR plural category for the count `n` in `locale` -- one of \"zero\", \"one\", \"two\", \"few\", \"many\" or \"other\". Support the locales \"en\", \"pl\", \"ru\" and \"ja\".", "domain": "internationalisation", "module": "plurals.py", "suite_sha256_at_registration": "c21a69a4426d7234feabecdeca41181b68468253af42c05bc6f5c5d3e91b4ada", "task": "c005", "why": "A named public standard with famous edge cases. If a specialist file cannot help here it is hard to say where it would."}
{"agent": "engineering-identity-access-engineer", "brief": "Write the signed session token used by our internal admin tool. Expose:\n\n    issue(subject: str, secret: str, now: int, ttl: int = 900) -> str\n    verify(token: str, secret: str, now: int) -> str\n\n`issue` returns a token string. `verify` returns the subject the token was issued for, or raises `ValueError` if the token is not valid at time `now`. `now` is an int epoch second. Tokens are short-lived: `ttl` is how many seconds a token stays valid.", "domain": "identity", "module": "tokens.py", "suite_sha256_at_registration": "41a0cf6a4e2ca6078df3a9ece991f82c8a1f3278fd2d6b55b459f8b8e1884937", "task": "c006", "why": "What a signature must COVER is never stated, and a token that signs only the subject round-trips perfectly."}
