{
  "runner": "subagent",
  "model": "claude-opus-5",
  "recorded_at": "2026-08-16",
  "protocol": "v2-line-citation",
  "tasks_sha256": "091d73e366c47940e9f6aee0ed7e5f6e23b17df113d8e6998b00ad69a3289c60",
  "notes": "HARD TIER ONLY: b005-b008, 4 tasks x 3 conditions, 12 blind subagents. The easy tier is answered by 2026-08-16-subagent-pilot12-v2. Output contract held 12/12. Answers stored verbatim. Subagents found seven defects the answer key does not list, all of them real -- see docs/behaviour-evaluation.md.",
  "answers": {
    "none": {
      "b005": "FINDING: L16: Wrapping created_at in date_trunc makes the predicate non-sargable, so events_tenant_created can only be probed on tenant_id and every row for that tenant is scanned instead of a created_at range.\nFINDING: L31: Filtering on created_at alone cannot use events_tenant_created because tenant_id is its leading column, so this scans all ~80M rows and then de-duplicates them.\nFINDING: L46: NOT IN over a subquery that can yield NULL manager_id (any actor with no manager) makes the whole predicate UNKNOWN for every row, so the query returns nothing.\nDONE: 3",
      "b006": "FINDING: L13: Each of the 12 processes opens its own pool of up to 40 connections, so a backlog drives ~480 backend connections and blows past Postgres's connection limit.\nFINDING: L21: Retries run inline in the worker instead of re-queueing the row, so one dead endpoint parks a worker and its claimed batch for up to ~90 seconds per message and the queue backs up behind it.\nFINDING: L34: The exponential backoff has no jitter (random is imported but never used), so all workers retry in lockstep and stampede the endpoint the moment it recovers.\nFINDING: L43: The claim UPDATE's subselect lacks FOR UPDATE SKIP LOCKED and the outer statement never re-checks state = 'pending', so all 12 workers block on the same 100 rows and then re-claim and re-deliver them.\nDONE: 4",
      "b007": "FINDING: L10: `parseFacets(rawFacets)` is evaluated eagerly on every render — not once per mount as the comment claims — because it is passed as `useState`'s value instead of a lazy initializer function.\nFINDING: L14: `filters` is rebuilt as a new object (with a new `facets` array) on every render, so it never compares equal and keeps the search effect's dependencies permanently dirty.\nFINDING: L16: `debounce` is called during render, so every render produces a brand-new debounced function with its own empty timer, and no keystroke is ever actually coalesced.\nDONE: 3",
      "b008": "FINDING: L18: Reset tokens are built with `random.choices`, a non-cryptographic Mersenne Twister PRNG whose future outputs are predictable from a few observed tokens.\nFINDING: L26: Returning a 404 for an unrecognized email turns this endpoint into an account-enumeration oracle.\nFINDING: L43: The new password is never validated, so a request that omits it resets the account to the empty password.\nFINDING: L52: Passwords are stored as a single unsalted SHA-256 digest rather than a slow salted hash such as bcrypt or argon2.\nDONE: 4"
    },
    "current": {
      "b005": "FINDING: L16: Wrapping `created_at` in `date_trunc('day', ...)` makes the predicate non-sargable, so the `(tenant_id, created_at)` index cannot restrict to the day and Postgres reads every row for the tenant instead of a range scan.\nFINDING: L31: The filter is on `created_at` alone, which no index supports (the composite index leads with `tenant_id`), forcing a sequential scan and dedupe over all ~80M rows.\nFINDING: L46: `NOT IN` against a subquery whose `manager_id` can be NULL makes the whole predicate UNKNOWN for every row, so the query correctly returns zero rows as soon as any actor has no manager.\nDONE: 3",
      "b006": "FINDING: L13: `max_size=40` sits in a pool that exists once per process, so the 12 workers can open 480 Postgres connections (and hold 48 permanently idle), far above a normal server connection limit.\nFINDING: L23: the POST carries no idempotency or delivery-ID key, so any retry after a read timeout the endpoint actually processed delivers a duplicate.\nFINDING: L26: every 4xx is classified as permanently undeliverable, so 429 and 408 responses (and their `Retry-After`) silently discard the delivery instead of deferring it.\nFINDING: L34: the entire retry schedule is slept through inside the worker — up to 31.5s of un-jittered sleeps plus 60s of timeouts per message, including a wasted final sleep after the last attempt — so one dead endpoint blocks the whole claimed batch for hours rather than returning the row to the queue with a scheduled next-attempt time.\nFINDING: L42: the claim UPDATE writes `'sending'` with no lease expiry, claim timestamp, or attempt counter, so rows held by a stalled or crashed worker become invisible to every other worker and are never reclaimed.\nFINDING: L44: the claiming subselect lacks `FOR UPDATE SKIP LOCKED`, so all 12 workers target the same oldest 100 pending rows and serialize and deadlock on each other's row locks instead of taking disjoint work.\nDONE: 6",
      "b007": "FINDING: L10: parseFacets(rawFacets) is passed as a computed value rather than a lazy initializer, so the expensive parse re-runs on every render instead of once per mount as the comment claims.\nFINDING: L16: debounce() is called during render, so each render creates a brand-new debounced function with its own 300ms timer and no keystroke is ever coalesced or cancelled.\nFINDING: L28: the effect's dependency array includes `filters`, a fresh object literal built every render, so the search re-fires on every render rather than only when the query changes.\nFINDING: L34: the search input has no accessible name — only a placeholder — leaving it unlabeled for screen readers.\nDONE: 4",
      "b008": "FINDING: L18: Reset tokens are generated with `random.choices`, a non-cryptographic Mersenne Twister PRNG whose output an attacker can predict from observed tokens, so tokens should come from `secrets`.\nFINDING: L26: Returning 404 with \"no account with that email\" turns the reset endpoint into an account-enumeration oracle; it should return the same 202 response whether or not the address exists.\nFINDING: L43: The new password is accepted with no validation at all, so a missing or empty `password` field silently sets the account's password to the hash of the empty string.\nFINDING: L46: The query loads every unexpired reset token in the table and scans it in Python instead of filtering on `token_hash`, so each confirm request costs a full scan that grows with traffic.\nFINDING: L52: Passwords are stored as unsalted single-round SHA-256, which is trivially reversible by rainbow table or GPU brute force and should be a salted slow KDF such as bcrypt, scrypt, or Argon2.\nDONE: 5"
    },
    "flattened": {
      "b005": "FINDING: L16: Wrapping created_at in date_trunc() makes the predicate non-sargable, so events_tenant_created can only seek on tenant_id and must then evaluate the function against every row of that tenant instead of doing a bounded created_at range scan.\nFINDING: L31: created_at is only the second column of events_tenant_created and no index leads with it, so this filter has no usable index and forces a full scan of all ~80M rows before the DISTINCT.\nFINDING: L46: NOT IN against the nullable manager_id column yields zero rows as soon as any manager_id is NULL, and it also blocks the anti-join plan, which is why this query is both empty and slow.\nDONE: 3",
      "b006": "FINDING: L13: min_size=4/max_size=40 per pool across 12 processes means up to 480 Postgres connections (and 48 permanently idle), far past a default server's max_connections, so a backlog exhausts the database's connection slots.\nFINDING: L26: every 4xx is treated as permanent, so the 429s and 408s a recovering endpoint returns once the backlog floods it silently discard those deliveries instead of retrying them.\nFINDING: L34: the backoff is a blocking in-process sleep with no jitter (random is imported and never used), so a worker is held for the whole ~90s ladder per message and all 12 processes retry the recovering endpoint in lockstep.\nFINDING: L36: exhausting the attempt ladder returns the same False as a permanent 4xx rejection, so an outage longer than the ~90s retry horizon marks every in-flight delivery permanently failed instead of rescheduling it in the queue.\nFINDING: L42: the claim sets state='sending' with no owner or claimed_at, so the up-to-100 rows a worker holds when it is restarted or killed are stranded in 'sending' with no lease expiry to reclaim them.\nFINDING: L44: the claiming subquery has no FOR UPDATE SKIP LOCKED, so all 12 workers select the same 100 oldest pending rows and serialize on row locks, producing lock waits and duplicate claims of the same deliveries.\nDONE: 6",
      "b007": "FINDING: L10: `parseFacets(rawFacets)` is passed to `useState` as an already-computed value rather than a lazy initializer, so the expensive parse re-runs on every render instead of once per mount as the comment claims.\nFINDING: L16: `debounce()` is called during render, so each render creates a brand-new debounced function with its own timer and no keystroke is ever actually coalesced.\nFINDING: L26: the effect returns no cleanup, so the pending debounced call is never cancelled and in-flight fetches are never aborted or ignored, letting a slow earlier response overwrite a newer one or land after unmount.\nFINDING: L28: the dependency array includes `filters`, a fresh object literal rebuilt on every render, so the effect re-fires on every render rather than only when the query changes.\nDONE: 4",
      "b008": "FINDING: L18: Reset tokens are generated with the `random` module's non-cryptographic Mersenne Twister PRNG, so an attacker who observes a few tokens can predict others and take over accounts.\nFINDING: L26: Returning 404 for unknown emails while returning 202 for known ones turns this endpoint into an account-enumeration oracle.\nFINDING: L43: The new password is accepted with no validation whatsoever, so a missing or empty `password` field silently resets the account to the hash of the empty string.\nFINDING: L52: Passwords are stored as a bare unsalted SHA-256 digest instead of a slow salted KDF such as bcrypt/scrypt/Argon2, making offline cracking and rainbow-table lookup trivial.\nDONE: 4"
    }
  }
}
